spring:

  datasource:
    url: jdbc:postgresql://localhost:24870/smarthouse
    driver-class-name: org.postgresql.Driver
    username: smarthouse
    password: smarthouse

mqtt:
  server-in: ${MQTT_HOST:localhost}
  server-out: ${MQTT_HOST:localhost}
  msgSavingEnabled: true
  msgSendingEnabled: false

  # UNCOMMENT TO LOG HIBERNATE DB REQUESTS
#  jpa:
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type.descriptor.sql: trace
#    org.hibernate.orm.jdbc.bind: trace

