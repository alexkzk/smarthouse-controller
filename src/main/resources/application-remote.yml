spring:
  datasource:
    url: jdbc:postgresql://192.168.1.77:24870/smarthouse

logging:
  filepath: ./log
  level:
    org:
      hibernate:
        SQL: debug

mqtt:
  server: ${MQTT_HOST:localhost}
  msgSavingEnabled: false
  msgSendingEnabled: false
